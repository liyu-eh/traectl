traectl-CLI 技术栈与架构文档

版本 3.1.0 · Python ≥ 3.11 · 6116 行源码 · 172 测试 · CDP JavaScript 注入控制

# 1. 📋 技术栈

以下表格列出 traectl-CLI 实际采用的核心技术及其用途。

注：pyproject.toml 中显式依赖只有 typer 和 rich；websockets 为运行时直接导入，pytest 为测试依赖。

# 2. 🏗️ 三层架构

traectl-CLI 采用清晰的三层架构设计，从顶层 CLI 到底层 CDP 通信逐层隔离职责：

## 实际调用链路

submit 命令示例：【用户的 submit 命令】→ Typer 解析 → solo_session(连接池) → ChatMixin.type_message + TaskMixin.submit_task

## 工程统计

# 3. 📦 模块职责一览

以下表格列出 traectl-CLI 全部核心模块及其一句话职责描述。

# 4. 💻 CLI 命令参考

以下按功能分组列出全部 34 个 CLI 命令。默认输出 JSON，加 --human 切换可读格式。

## 📦 任务控制

## 🤖 模型管理

## 💬 聊天交互

## 📁 文件变化

## 💻 终端操作

## 📸 媒体与弹窗

## 🔧 Git

## 🩺 诊断与自省

## 🛠️ 维护操作

## ⚙️ 配置管理

## 📂 工作区管理

# 5. 🔄 核心流程

## 5.1 任务提交流程（submit）

以下步骤展示从输入 prompt 到获取 SOLO 响应的完整链路：

## 5.2 自动弹窗处理流程（auto-recover）

auto-recover 命令自动扫描并处理页面上出现的各类弹窗，支持 8 种弹窗类型：

## 5.3 模型切换流程（关键 JS 注入场景）

## 5.4 响应等待状态机（ResponseWaiter）

ResponseWaiter 是 submit/send 命令的核心等待引擎，542 行独立模块。其状态转换如下：

# 6. 📡 CDP 通信机制

## 6.1 连接架构

CDP（Chrome DevTools Protocol）是 traectl 与 Trae CN 浏览器实例通信的核心协议。通过 WebSocket 连接 Chrome DevTools，发送 Runtime.evaluate 等命令操控页面。

端口与连接配置：

## 6.2 页面匹配策略

从 CDP 的页面列表中匹配目标页面时采用三级降级策略：

## 6.3 心跳与重连

为保持连接稳定，CDP 通信层实现了心跳和自动重连机制：

## 6.4 主要 CDP 命令

traectl 使用以下 CDP 命令与 Trae CN 浏览器交互：

## 6.5 连接池机制