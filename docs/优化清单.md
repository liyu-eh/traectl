# 🛠️ traectl-CLI 优化清单

> 生成时间：2026-05-21  
> 基于架构文档与代码对照分析，共 **19 项**优化点

---

## 🔴 P0 — 文档同步（影响理解，推荐立即更新）

| # | 优化项 | 类型 | 当前状态 | 工作量 |
|---|--------|------|---------|--------|
| 1 | **模块数修正**：文档写「7 个 Mixin」，实际 8 个（+HealthMixin） | 文档错误 | ❌ 待修复 | 5 min |
| 2 | **补充缺失模块**：文档遗漏 `project_manager.py`、`mixins/base.py`、`js_templates.py`、`response_waiter.py`、`health_mixin.py` | 文档遗漏 | ❌ 待补充 | 15 min |
| 3 | **workspace_manager 拆分说明**：已拆为 `project_manager.py` + `workspace_manager.py` | 文档过时 | ❌ 待更新 | 5 min |
| 4 | **ConnectionState 迁移**：文档仍写「在 test_config.py」，实际已迁至 `cdp_client.py` | 文档错误 | ❌ 待修正 | 5 min |
| 5 | **等待策略章节更新**：改为 `ResponseWaiter` 状态机架构说明 | 文档过时 | ❌ 待重写 | 20 min |
| 6 | **补充 Task Queue 说明**：新增 `ensure_idle()` 自动排队等待机制 | 文档遗漏 | ❌ 待补充 | 10 min |
| 7 | **补充健康检查说明**：新增 `get_health_info()` + `HealthMixin` | 文档遗漏 | ❌ 待补充 | 10 min |
| 8 | **补充 JS 模板抽取说明**：806 行 JS 字符串已抽出到 `js_templates.py` | 文档遗漏 | ❌ 待补充 | 10 min |
| 9 | **补充 exit_code 字段**：`StandardResponse` 新增 `exit_code` | 文档遗漏 | ❌ 待补充 | 5 min |

> 📝 **优先级说明**：P0 是文档与代码不同步的问题。虽然不影响功能，但新同学接入或回顾架构时会产生混淆。

---

## 🟡 P1 — 架构优化（提升可维护性 & 代码组织）

| # | 优化项 | 影响 | 当前问题 | 方案建议 | 工作量 |
|---|--------|------|---------|---------|--------|
| 10 | **拆分 js_templates.py (806 行)** | 可维护性 🟢 | 所有 JS 字符串堆在一个文件，定位困难 | 按 Mixin 拆子模块：`js_templates/chat.py`、`js_templates/task.py`、`js_templates/editor.py`… | 2h |
| 11 | **完善 TraeSoloProtocol** | 类型安全 🟡 | `base.py` 只定义了 5/8×N 个方法 | 补充全部公开方法签名，或改用 `typing.Protocol` 大接口自动继承 | 1h |
| 12 | **抽取辅助方法到独立模块** | 架构清晰 🟡 | `_normalize_content`、`_is_placeholder`、`_is_folder_prompt`、`_compute_poll_interval` 在 chat_mixin.py 中被 ResponseWaiter 依赖 | 新建 `utils/content_utils.py` 迁移这些静态工具方法 | 1h |
| 13 | **抽取文件监控到独立模块** | 架构清晰 🟡 | `_monitor_file_mtime` 在 chat_mixin.py 中，被 ResponseWaiter 调用 | 新建 `utils/fs_utils.py` 迁移 | 30min |
| 14 | **拆分 cli.py (1193 行)** | 可维护性 🟡 | Typer 命令定义 + 格式化 + 错误处理混在一起 | 按命令领域拆分：`commands/task_commands.py`、`commands/model_commands.py`、`commands/config_commands.py` | 3h |
| 15 | **connection_pool 参数可配置化** | 灵活性 🟡 | `IDLE_TIMEOUT=300`、`SCAN_INTERVAL=60`、`MAX_POOL_SIZE=10` 为模块常量 | 改为从 `config.py` 读取 + 环境变量可配 | 30min |
| 16 | **#9 弃用替代方案** | 完整性 🟡 | 之前尝试 Annotated shared params 因 Typer 不兼容放弃 | 调研替代方案：dataclass 配置注入、Typer callback 统一参数 | 2h ⚠️ 高风险 |

---

## 🟢 P2 — 小优化 & 锦上添花

| # | 优化项 | 影响 | 问题 | 工作量 |
|---|--------|------|------|--------|
| 17 | **HealthMixin 合并回 Controller** | 精简度 🟢 | 仅 71 行 / 1 个导出方法，独立 Mixin 收益不大 | 30min |
| 18 | **退出码统一** | 一致性 🟢 | `response.py` 定义 `EXIT_SUCCESS` 等，`config.py` 的 `CONSTANTS` 也有涉及 | 30min |
| 19 | **添加集成测试用例** | 质量 🟢 | 165 个全单元测试，无真实 CDP 连接测试 | 1h |

---

## 📊 统计

| 优先级 | 数量 | 预估总工时 |
|--------|------|-----------|
| 🔴 P0 文档同步 | 9 项 | ~1.5h |
| 🟡 P1 架构优化 | 7 项 | ~10h |
| 🟢 P2 小优化 | 3 项 | ~2h |
| **合计** | **19 项** | **~13.5h** |

---

## ✅ 推荐执行顺序

```
第 1 步 → P0-1~P0-9  更新架构文档（文档先行，1.5h）
第 2 步 → P1-12~13    抽取工具方法（底层依赖，1.5h）
第 3 步 → P1-15        连接池参数可配（独立模块，30min）
第 4 步 → P1-10        拆分 js_templates.py（独立模块，2h）
第 5 步 → P1-14        拆分 cli.py（最大改动，3h）
第 6 步 → P1-11        完善 TraeSoloProtocol（类型安全，1h）
第 7 步 → P2-17~19     小优化收尾（2h）
第 8 步 → P1-16        #9 替代方案（高风险最后，2h）
```

> ⚠️ 所有代码修改必须通过 `traectl submit` 委托给 Trae CN 执行，每完成一项验证测试通过再继续下一项。

---

*本清单由架构文档与代码对照生成，按需选取执行即可。*
